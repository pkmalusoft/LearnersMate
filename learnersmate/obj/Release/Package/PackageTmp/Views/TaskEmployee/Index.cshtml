@model IEnumerable<learnersmate.Models.TaskEmployee>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layouts.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurrentEmployee.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CurrentTaskList.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CurrentEmployee.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CurrentTaskList.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TaskEmployeeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TaskEmployeeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TaskEmployeeID })
        </td>
    </tr>
}

</table>

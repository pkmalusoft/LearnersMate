@model IEnumerable<learnersmate.Models.TaskEmployee>
<div class="task-item-users" data-assigned-users="@ViewBag.EmployeeIDList">
    @foreach (var item in Model)
    {
        <a class="assigned-user">
            <div class="profile-picture assigned-user bg-gradient bg-primary float-right d-flex">
                <img class="list-thumbnail" src="@item.CurrentEmployee.Photo" width="30" height="30" alt="@item.CurrentEmployee.Name">
            </div>
        </a>
    }
</div>
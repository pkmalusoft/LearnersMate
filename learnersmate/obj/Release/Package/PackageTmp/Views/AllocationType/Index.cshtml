@model IEnumerable<learnersmate.Models.AllocationType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layouts.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurrentAllocationGroup.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CurrentAllocationGroup.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AllocationTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AllocationTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AllocationTypeID })
        </td>
    </tr>
}

</table>

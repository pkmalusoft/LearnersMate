@model IEnumerable<learnersmate.Models.NotificationParent>
<div class="task-item-users" data-assigned-users="@ViewBag.ParentIDList">
    @foreach (var item in Model)
    {
        <a class="assigned-user">
            <div class="profile-picture assigned-user bg-gradient bg-primary float-right d-flex">
                <img class="list-thumbnail" src="@item.CurrentParent.Photo" width="30" height="30" alt="@item.CurrentParent.Name">
            </div>
        </a>
    }
</div>